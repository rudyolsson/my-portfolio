export interface TechIcon {
  src: string;
  name: string;
}

export interface Project {
  imgURL: string;
  title: string;
  description: string;
  skills: string;
  siteURL?: string;
  bgColor?: string;
}
